// use algonaut::algod::v2::Algod;
// use anyhow::Result;
// use std::fs::{File, OpenOptions};
// use std::io::{self, BufRead, Write};
// use std::path::Path;

// use crate::flows::create_dao::storage::load_dao::DaoAppId;

// /// Assumes image bytes already compressed
// /// If too large, the backend will reject it
// pub async fn set_dao_image(algod: &Algod, app_id: DaoAppId, image: Vec<u8>) -> Result<()> {

// }
